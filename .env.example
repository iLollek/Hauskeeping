# Flask
SECRET_KEY=ein-langer-zufaelliger-string
HOST=0.0.0.0
PORT=5000

# Datenbank (PostgreSQL empfohlen, SQLite als Fallback)
# DATABASE_URL=postgresql://user:passwort@localhost:5432/hauskeeping
DATABASE_URL=sqlite:///hauskeeping.db

# Reverse Proxy
USE_PROXY=false
PROXY_PREFIX=/hauskeeping

# SMTP (E-Mail Notifications)
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=user@example.com
MAIL_PASSWORD=geheim
MAIL_USE_TLS=true
MAIL_DEFAULT_SENDER=hauskeeping@example.com

# VAPID (Web Push Notifications)
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIM_EMAIL=admin@example.com
